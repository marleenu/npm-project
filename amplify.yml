version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands: ["npm run"]
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
